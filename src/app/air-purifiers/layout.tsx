import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Best Air Purifiers Compared 2025-2026 | The Family Pick",
    description: "Compare air purifiers by filtration, coverage area, noise level, and price. Breathe easier at home.",
    alternates: { canonical: "https://thefamilypick.com/air-purifiers" },
};

export default function CategoryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
